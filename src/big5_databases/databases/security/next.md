# next.md

## jsonpaths:

- [ ] replace_in_data ub @src/big5_databases/databases/security/jsonpath_extractor.py seems to use hard coded paths. that
should be generic. you had the list of paths for the user-id and the data fields somewhere- did you delete that?

## security tests

- [ ] all those security test modules in the project root. MOVE THEM SOMEWHERE ELSE!
